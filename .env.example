# Only required for the AI advisor (src/advisor.py). Tracking + analytics work without it.
ANTHROPIC_API_KEY=

# Optional: future production data providers (not used by the default yfinance provider)
FINNHUB_API_KEY=
ALPACA_API_KEY=
ALPACA_API_SECRET=

# Risk-free rate (annualized) used for Sharpe ratio, e.g. 0.045 = 4.5%
RISK_FREE_RATE=0.045

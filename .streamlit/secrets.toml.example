# Copy this file's CONTENTS into the Streamlit Community Cloud "Secrets" box
# (App → Settings → Secrets) when you deploy, then replace the placeholder holdings
# with your real positions. Do NOT commit a real secrets.toml.
#
# For local testing you can save a real copy as .streamlit/secrets.toml — it is
# gitignored so it won't be pushed.

# Password that gates the whole dashboard.
app_password = "change-me-to-something-strong"

# Optional: enables the AI rebalancing brief. Leave blank to disable.
ANTHROPIC_API_KEY = ""

# Your portfolio, kept out of the repo. Replace these placeholder rows with yours.
# (CASH is a special ticker priced at $1, so shares = dollars held.)
holdings_csv = """
ticker,shares,cost_basis
AAPL,10,165.00
MSFT,5,330.00
VOO,12,400.00
CASH,5000,1.00
"""
